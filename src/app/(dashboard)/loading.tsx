import { RouteLoading } from "@/components/ui/RouteLoading";

export default function DashboardLoading() {
  return (
    <RouteLoading
      title="Carregando operacao"
      description="Estamos preparando seu painel, metricas e modulos internos."
    />
  );
}
