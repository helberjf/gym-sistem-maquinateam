import { RouteLoading } from "@/components/ui/RouteLoading";

export default function Loading() {
  return (
    <RouteLoading
      title="Carregando o sistema"
      description="Estamos organizando a experiencia para voce."
    />
  );
}
